version: "3.8"

services:
  qrai:
    image: hanwyn/qrai:latest
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - qrai-data:/data
    restart: unless-stopped

volumes:
  qrai-data:
